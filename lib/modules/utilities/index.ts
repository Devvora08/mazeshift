export type { UtilityType } from './types';

// Logic (acquiring, casting, inventory) and rendering land here once utilities are built —
// see lib/modules/scramble for the reference shape (types.ts + logic.ts + index.ts barrel).
